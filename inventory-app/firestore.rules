rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Require authenticated users for app access.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
